*Definition* A process is an instance of a computer program that is being executed.

Shell runs child process. 
Each child process is a `fork` of the shell (when child exits, shell is still
operational).
